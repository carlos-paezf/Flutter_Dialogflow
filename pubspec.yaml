name: flutter_and_dialogflow
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.0.4
  path_provider: ^2.0.2
  provider: ^5.0.0
  cupertino_icons: ^1.0.2
  bubble: ^1.2.1
  intl: ^0.17.0
  flutter_dialogflow: ^0.1.3
  image: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"

flutter:
  uses-material-design: true
  assets:
    - assets/dialogflow/service.json
    - assets/dialogflow/materias-ghuf-dc7aa8b181dc.json
    - assets/images/user.png
    - assets/images/logo.png